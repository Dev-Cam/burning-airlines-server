
<h2>Flights Index</h2>

<ul>
    <% @flights.each do |flight| %>
        <li>
            <%= flight.date %> | <%= link_to flight.flight_number, flight_path(flight.id) %> | <%= flight.origin %> > <%= flight.destination %> | <%= flight.airplane.name %> | TO DO: seats available
        </li>
        <% end %>
    </ul>

<%= link_to 'Create New Flight'%>    


